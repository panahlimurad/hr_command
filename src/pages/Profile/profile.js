import React from "react";
import ProfileContainer from "../../features/Components/Profile/ProfileContainer/ProfileContainer";

const Profile = () => {
  return (
    <div className="h-full w-[95%] flex items-center justify-center">
      <ProfileContainer />
    </div>
  );
};

export default Profile;
